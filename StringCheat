Int to String
int i=10;  
String s=String.valueOf(i);//Now it will return "10"  
					OR
int i=10;  
String s=Integer.toString(i);//Now it will return "10"  

String to Int
int i=Integer.parseInt("200");  

trim()- used to remove starting and ending spaces in string
for ex= String s=" hello world "; ---> "hello world"

 String s="SachinTendulkar";    
 System.out.println("Original String: " + s);  
 System.out.println("Substring starting from index 6: " +s.substring(6));//Tendulkar    
 System.out.println("Substring starting from index 0 to 6: "+s.substring(0,6)); //Sachin  
 
 
 
 //converting String of array in to intergers
		ArrayList<String> subString=new ArrayList<String>();
		ArrayList<Integer> array=new ArrayList<>();
		for(String s:subString) 
		{
			array.add(Integer.parseInt(s));
		}